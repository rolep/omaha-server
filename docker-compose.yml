---
version: '2.4'

services:
  db:
    extends:
      file: docker-compose.common.yml
      service: db

  redis:
    extends:
      file: docker-compose.common.yml
      service: redis

  web:
    extends:
      file: docker-compose.common.yml
      service: web
    depends_on:
      - db
      - redis
